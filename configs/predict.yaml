# Prediction configuration
run_id: "pred_run"
model_id: "rfdetr_tuned/rfdetr_large_aug2"
#model_id: "yolo_tuned/yolo11x_v2"
#model_id: "yolo_pretrained/yolov5l"
#model_id: "rfdetr_pretrained/small"
output_dir: "runs/predict"
device: "cuda"

thresholds:
  conf: 0.35
  iou: 0.50

tracking:
  type: "bytetrack"  # "none" | "bytetrack"
  tracker_yaml: "tracker.bytetrack.yaml"  # YOLO-only (Ultralytics)
  params: {}  # Supervision ByteTrack params (RF-DETR)

videos_dir: "data/videos"
videos:
  - "vid16.mp4"
  - "vid17.mp4"
  - "vid18.mp4"

probe_frames: 0

export:
  save_video: true
  save_raw: true
  save_counts_json: true

debug: false

preview:
  enabled: true
  every_n_frames: 5
  max_width: 1200

line:
  name: "main_line"
  coords: [727.0, 459.0, 1133.0, 549.0] # x1, y1, x2, y2
  default_resolution: [1920, 1080]  # width, height

greyzone_px: 10


oscillation_window_frames: 0
trajectory_len: 40
