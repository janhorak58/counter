# Evaluation configuration
gt_dir: data/counts_gt
runs_dir: runs/to_evaluate
out_dir: runs/eval

only_completed: true

videos_dir: ""  # leave empty if you do not want rate-based metrics

filters:
  run_ids: []
  backends: [rfdetr]
  variants: [tuned]
  model_ids: []

charts:
  enabled: true
